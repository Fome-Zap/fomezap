# ===================================
# VARIÁVEIS DE AMBIENTE - BACKEND
# ===================================
# Copie este arquivo para .env e preencha com seus valores reais

# MongoDB (Docker local - acesso via host)
MONGODB_URI=mongodb://admin:fomezap123@127.0.0.1:27017/fomezap?authSource=admin
# Para MongoDB sem autenticação:
# MONGODB_URI=mongodb://127.0.0.1:27017/FomeZap
# Para produção (MongoDB Atlas):
# MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/fomezap

# JWT Secret (ALTERE PARA ALGO ÚNICO E SEGURO!)
JWT_SECRET=seu-jwt-secret-super-seguro-aqui-change-me-PROD

# Porta do servidor
PORT=5000

# Ambiente (development ou production)
NODE_ENV=development

# CORS Origins (separados por vírgula)
# Desenvolvimento:
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:80
# Produção (adicione seus subdomínios):
# CORS_ORIGINS=https://familia.fomezap.com,https://outro-tenant.fomezap.com

# ===================================
# INSTRUÇÕES PARA DEPLOY
# ===================================
# 
# RENDER.COM:
# Configure estas variáveis no painel do Render em "Environment"
# - MONGODB_URI: sua URI do MongoDB Atlas
# - JWT_SECRET: um valor aleatório e seguro
# - NODE_ENV: production
# - PORT: (Render define automaticamente)
#
# RAILWAY.APP:
# Use o arquivo railway.json ou configure via CLI/Dashboard
#
