# ===================================
# VARIÁVEIS DE AMBIENTE - PRODUÇÃO
# ===================================
# Configure estas variáveis no Render.com

# MongoDB Atlas (SUBSTITUA com sua string de conexão)
MONGODB_URI=mongodb+srv://SEU_USUARIO:SUA_SENHA@cluster0.xxxxx.mongodb.net/fomezap?retryWrites=true&w=majority

# JWT Secret (GERE UM VALOR ALEATÓRIO E SEGURO!)
# Execute: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=seu-jwt-secret-muito-seguro-e-aleatorio-aqui-CHANGE-ME

# Porta (Render define automaticamente)
PORT=5000

# Ambiente
NODE_ENV=production

# CORS Origins (adicione seu domínio da Vercel)
CORS_ORIGINS=https://seu-app.vercel.app,https://fomezap.vercel.app

# ===================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# ===================================
# 
# 1. No MongoDB Atlas:
#    - Crie um cluster gratuito
#    - Em "Database Access", crie um usuário
#    - Em "Network Access", adicione 0.0.0.0/0 (ou IP específico)
#    - Copie a connection string
#
# 2. No Render.com:
#    - Vá em Environment
#    - Cole cada variável acima (sem os comentários #)
#    - MONGODB_URI: cole sua string do Atlas
#    - JWT_SECRET: gere um valor aleatório
#    - NODE_ENV: production
#    - CORS_ORIGINS: URL do seu frontend na Vercel
#
