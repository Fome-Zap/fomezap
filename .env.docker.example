# Docker Compose Environment Variables
# Copie este arquivo para .env.docker e configure suas credenciais

# MongoDB
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=ALTERE_AQUI_SUA_SENHA_SEGURA
MONGO_DATABASE=fomezap

# Backend
NODE_ENV=production
PORT=5000
JWT_SECRET=GERE_UM_TOKEN_ALEATORIO_SEGURO_AQUI
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:80

# Frontend
VITE_API_URL=http://localhost:5000/api

# ===================================
# INSTRUÇÕES
# ===================================
# 1. Copie este arquivo: cp .env.docker.example .env.docker
# 2. Edite .env.docker e altere TODAS as senhas
# 3. Para gerar JWT_SECRET forte:
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
